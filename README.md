# Joshua Knott Portfolio

Responsive portfolio website built as real HTML, CSS, SVG, and TypeScript components.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Project Links

The visible project cards are currently linked to local placeholders:

- `#memvella-project`
- `#polis-project`
- `#society-website-system`

Replace those `href` values in `src/main.ts` when the final project URLs are ready.

# Joshua Knott portfolio

Static portfolio: index.html, styles.css, main.js, assets/, and data/. No package manifest or build step is present.

- Inspect the current page and assets before changing layout or project copy. Ground claims, dates, links, and screenshots in current project evidence; use compact factual wording.
- Preview over HTTP with an available static server. Check affected desktop/mobile views, keyboard use, browser console, navigation, and links. `node --check main.js` checks JavaScript syntax; do not invent an npm test/build command.
- .github/workflows/update-stats.yml maintains generated statistics. Preserve its data and reconcile remote automation changes before authorized publication.
- Preserve CNAME, sitemap.xml, robots.txt, social metadata, and deployment wiring unless the task requires changing them.
- When publication is requested, verify the deployed page and relevant download/remote links. A local preview does not establish public availability.
